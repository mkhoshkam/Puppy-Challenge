import 'package:flutter/material.dart';

final ThemeData themeData = ThemeData(
  primaryColor: Colors.orange,
  highlightColor: Colors.orange,
  progressIndicatorTheme:const ProgressIndicatorThemeData(color: Colors.orange),
  useMaterial3: true,
);

const cardRadius = 8.0;
const buttonTextColor = Colors.orange;


const buttonTextStyle =  TextStyle(color: buttonTextColor);
const filterButtonTextStyle =  TextStyle(color: Colors.red,fontSize: 12);